# use_frameworks!

platform :ios, '9.0'

inhibit_all_warnings!

project 'MLMNetWork.xcodeproj'
target 'MLMNetWork_Example' do
  pod 'MLMNetWork', :path => '../'

  target 'MLMNetWork_Tests' do
    inherit! :search_paths

    
  end
end
